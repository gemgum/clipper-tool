# Salin ke .env dan isi. .env di-gitignore.

# Diperlukan hanya untuk mode hybrid / online (scoring & judul via Claude)
ANTHROPIC_API_KEY=

# Opsional: override lokasi binary & model (default terdeteksi otomatis)
# CLIPPER_WHISPER_BIN=./bin/whisper-cli
# CLIPPER_WHISPER_MODEL=./models/ggml-small.bin
# CLIPPER_WORKER_BIN=./bin/clipper-worker
# CLIPPER_FFMPEG_BIN=ffmpeg
# CLIPPER_DATA_DIR=./data
